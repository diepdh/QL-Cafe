# 🧰 SKILL REQUIREMENTS: [Tên dự án]
## Danh sách kỹ năng và tools Coder cần có
**Tạo bởi:** Brain | **Ngày:** [YYYY-MM-DD]

---

> Đây là checklist để Coder self-assess trước khi bắt đầu.
> Nếu thiếu skill nào, báo Brain NGAY — đừng tiếp tục.

---

## 🔧 TOOLS CẦN THIẾT

```
□ Node.js [version]+        → node --version
□ npm [version]+            → npm --version
□ Git                       → git --version
□ VSCode / IDE              → (hoặc coding agent tương đương)
□ Terminal / Shell access   → Cần để chạy commands
```

---

## 💻 TECHNICAL SKILLS

### Must Have (P0 — Bắt buộc):

```
□ React / Next.js App Router   → Viết được Server + Client components
□ TypeScript cơ bản            → Định nghĩa interfaces, props
□ Tailwind CSS                 → Responsive layouts, custom classes
□ File system operations       → Tạo files, folders, edit
□ npm commands                 → install, run dev, run build
```

### Should Have (P1 — Nên có):

```
□ [Skill theo project — e.g. Framer Motion]   → [Mức độ cần]
□ [Skill theo project — e.g. Supabase]        → [Mức độ cần]
□ [Skill theo project — e.g. NextAuth]        → [Mức độ cần]
```

### Nice to Have (P2 — Có thì tốt):

```
□ [Skill thêm 1]
□ [Skill thêm 2]
```

---

## 📚 TÀI LIỆU THAM KHẢO

*Coder nên bookmark các links này trước khi bắt đầu:*

| Resource | URL | Khi nào dùng |
|----------|-----|-------------|
| Next.js Docs | https://nextjs.org/docs | App Router questions |
| Tailwind Docs | https://tailwindcss.com/docs | Class references |
| [Tech khác] | [URL] | [Khi nào] |

---

## ⚠️ PRE-FLIGHT CHECK

Trước khi bắt đầu TASK đầu tiên, verify:

```bash
# Kiểm tra environment
node --version    # Phải >= 18
npm --version     # Phải >= 8

# Tạo project test (optional nhưng khuyến khích)
npx create-next-app@latest test-project --typescript --tailwind --app --src-dir
cd test-project && npm run dev
# → Nếu chạy được ở localhost:3000: ✅ Ready
# → Xóa test-project đi sau khi verify
```

---

*Nếu thiếu bất kỳ P0 skill nào → Báo Brain ngay, đừng bắt đầu.*
*Brain sẽ tìm giải pháp hoặc điều chỉnh tech stack.*
